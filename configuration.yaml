# Database settings.
database:
  # the name of the JDBC driver, mysql in our case
  driverClass: com.mysql.cj.jdbc.Driver
  # the username
  user: root
  # the password
  password: thalapathy3577
  # the JDBC URL; the database is called DWGettingStarted
  url: jdbc:mysql://localhost:3306/fos